<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ── Permissions declared by the SDK ──────────────────────────────────────
         Host apps that use the SDK will inherit these automatically via manifest merger.
         The host app still MUST request location permissions at runtime before calling
         YumaSdk.launchHome(). -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Location — required for map features and station discovery -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Bluetooth — required for BLE battery swap features -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />

    <uses-feature android:name="android.hardware.bluetooth" />

    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />


    <uses-feature android:name="android.hardware.camera"/>
    <uses-feature android:name="android.hardware.camera.autofocus"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.FLASHLIGHT"/>

    <uses-permission android:name="android.permission.VIBRATE" />
    <application>

        <!--
            SdkMainActivity — the SDK's internal root Activity.
            NOT exported: invisible to the host app's external callers.
            Launched exclusively via YumaSdk.launchHome(context).
        -->
        <activity
            android:name=".SdkMainActivity"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|mnc|colorMode|density|fontScale|fontWeightAdjustment|keyboard|layoutDirection|locale|mcc|navigation|smallestScreenSize|touchscreen|uiMode"
            android:exported="true"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"
            android:theme="@android:style/Theme.Material.Light.NoActionBar"/>

        <!--
            Map API Key — injected at runtime from YumaSdkConfiguration.mapApiKey.
            The placeholder value here will be replaced via manifestPlaceholders
            in the consuming app's build.gradle. This prevents the key from being
            hardcoded in the SDK's source.

            Host app must add to their build.gradle:
                manifestPlaceholders["YUMA_MAP_API_KEY"] = "<key from YumaSdk.getConfig().mapApiKey>"

            Or the SDK can inject it programmatically via a MetaData helper on Activity start.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value= "${YUMA_MAP_API_KEY}" />

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="General" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/yuma_logo" />

                <service
                    android:name="com.yumaoem.core.utils.notifications.data.fire_base_messaging.FireBaseService"
                    android:exported="true">
                    <intent-filter>
                        <action android:name="com.google.firebase.MESSAGING_EVENT" />
                    </intent-filter>
                </service>

        <service
            android:name=".feature_home.common.notification.NotificationService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">

            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="persistent_notification_for_user_awareness_on_token_expiry" />
        </service>

    </application>


</manifest>